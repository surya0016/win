1. Create a class student with data: Roll no., name, department and marks 
include function student details and update marks apart from init function 
create two object and evoke the function.

2. Create an employee class with attributes- eid, name, and a method that 
displays the employee id and name. Derive from empclass a class typist 
with an extra attribute speed of typing and a method that displays 
the employee id,name and speed of typing.


3. Write a python program to implement a super class Publication(title, price)
with a sub class Book(author, no_of_pages) and a sub class Magazine(issue_no).
The subclass will inherit parent class data. Each class 
should have a method called display() which will print class data.

